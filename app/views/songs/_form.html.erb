<%= form_for [@playlist, @song] do |f| %>
  <%= f.label :artist %>
  <%= f.text_field :artist %>

  <%= f.label :title %>
  <%= f.text_field :title %>

  <%= f.label :url %>
  <%= f.text_field :url %>

  <%= f.submit "Jam On" %>

<% end %>
